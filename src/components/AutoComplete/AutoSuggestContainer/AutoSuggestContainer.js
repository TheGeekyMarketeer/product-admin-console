import React from 'react'
import './AutoSuggestContainer.scss'

const AutoSuggestContainer = ({ children }) => (
    <div className="auto-suggest-container">
        {children}
    </div>
);

export default AutoSuggestContainer;